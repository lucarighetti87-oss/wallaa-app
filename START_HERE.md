# Wallaa Personal Safety Ecosystem v4.0.12 — START HERE

This is the consolidated pre-review test build.

## Required order
1. Deploy `Wallaa_Backend_v4_0_12_GitHub_ROOT.zip` to the existing `wallaa-safe-button-api` GitHub repository.
2. Wait for the existing Render service to become Live.
3. On Mac, create `.env.production` from `.env.production.example`.
4. Run `npm install`, `npm run build`, `npm run cap:sync`, `npm run ios:open`.
5. Xcode → Product → Clean Build Folder → real iPhone → Run.

Expected version: 4.0.12, build 13.
